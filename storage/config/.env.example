# =============================================================================
# doc-thor — Storage (Garage) environment
# =============================================================================
# These values are generated automatically during the first container start.
# Retrieve them from the Garage bootstrap output:
#
#   docker compose --env-file env/.env logs garage
#
# The secret key is printed only once; it is NOT recoverable afterwards.
# =============================================================================

STORAGE_ACCESS_KEY=                # Key ID  from: garage key create doc-thor-key
STORAGE_SECRET_KEY=                # Secret  from the same output
STORAGE_BUCKET=doc-thor-docs       # Single bucket; matches the value in garage.toml bootstrap
